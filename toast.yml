image: python:3.7-slim
tasks:
  packages-update:
    command:
      apt-get update
  packages-install:
    dependencies:
      - packages-update
    command:
      apt-get install -y gcc automake make libtool git curl
  install-node:
    dependencies:
      - packages-install
    command: |
      curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.35.3/install.sh | bash
      . ~/.nvm/nvm.sh
      nvm install node
  install:
    dependencies:
      - install-node
    input_paths:
      - Makefile
      - setup.py
      - README.md
      - package.json
      - package-lock.json
      - .install-typings-boto3.sh
    environment:
      CI: true
    command: |
      . ~/.nvm/nvm.sh
      mkdir src
      make install
  check-test:
    dependencies:
      - install
    input_paths:
      - .darglint
      - .flake8
      - pyproject.toml
      - pyrightconfig.json
      - src/aec/
      - tests/
    environment:
      CI: true
    command: |
      . ~/.nvm/nvm.sh
      make check test
